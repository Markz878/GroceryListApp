@inherits InputBase<string>
<div class="form-group">
    <label for="@id">@DisplayName:</label>
    <input id="@id" class="form-control" type="@type" @bind="@CurrentValueAsString" required placeholder="@DisplayName" spellcheck="false" autocomplete="off" autocorrect="off" autocapitalize="off"/>
</div>
@code {
    [Parameter] public string Id { get; set; } = string.Empty;
    [Parameter] public bool IsPassword { get; set; }

    private string id => Id ?? DisplayName?.Replace(" ", "-").ToLowerInvariant() ?? "";
    public string type => IsPassword ? "password" : "text";

    protected override bool TryParseValueFromString(string? value, out string? result, out string validationErrorMessage)
    {
        result = value;
        validationErrorMessage = "";
        return true;
    }
}
